// Import các mạng từ thirdweb
import { ethereum, sepolia } from "thirdweb/chains";

// Danh sách các mạng được hỗ trợ
export const supportedChains = [ethereum, sepolia];

// Mạng mặc định
export const defaultChain = sepolia;